server:
  port: 8091 # 避开现有：8081(otel)、8092(consumer)

spring:
  application:
    name: sca-provider # consumer 的 @FeignClient(name="sca-provider") 按此寻址
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # Nacos 主端口；gRPC 9848 由 SDK 自动推导，不要写进地址
